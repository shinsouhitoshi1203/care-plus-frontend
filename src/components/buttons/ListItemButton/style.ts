import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  fullButton: {
    width: "100%",
    display: "flex",
    justifyContent: "flex-start",
    gap: 20,
    minHeight: 58,
    borderRadius: 13,
  },

  fullButtonIcon: {
    marginLeft: 10,
  },
});
