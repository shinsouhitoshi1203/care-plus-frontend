## Get started

1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
   npm start
   ```

3. Hot reload: Press `r` in the terminal to reload the app, or shake your device to open the developer menu and select "Reload".
